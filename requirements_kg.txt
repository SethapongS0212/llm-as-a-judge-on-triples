# KG extraction dependencies
# Install with: pip install -r requirements_kg.txt --break-system-packages

# Core
beautifulsoup4>=4.12.0       # HTML parsing
networkx>=3.0                # Graph construction
pyvis>=0.3.2                 # Interactive visualization

# REBEL extractor (general baseline)
transformers>=4.40.0         # REBEL + ITER model loading
torch>=2.0.0                 # Inference backend

# ITER extractor (SciERC science-specific baseline)
# Install separately:
# pip install git+https://github.com/fleonce/iter

# LLM extractor (Qwen2.5)
bitsandbytes>=0.43.0         # 4-bit quantization
accelerate>=0.27.0           # device_map="auto" multi-GPU support

# Optional — better sentence splitting
# spacy>=3.7.0
# python -m spacy download en_core_web_sm