# Results — Scientific-Paper Knowledge-Graph Pipeline

> ### ⚠ SESSION 27: THE EVALUATION CRITERIA CHANGED
> Every precision number in this file uses the **old** CORRECT/PARTIAL/INCORRECT rubric and the
> **20-paper** corpus. As of 2026-09-05 the project evaluates on **six criteria (C1-C6)** over a
> **22-paper** corpus, via `ontology_eval.py`. These tables are still valid for what they measured
> (they are all *precision*), but C2/C5 add a **completeness** axis that none of them cover.
> See `hands_off.md` §27.


> **⭐ For a self-contained write-up — project overview, pipeline, both ontologies, both
> evaluation tracks, methodology, negative results and limitations in one file — see
> `RESULTS_REPORT.md`, generated by `build_results_report.py`. This file remains the full
> results archive, including superseded rounds.**
>
> **⛔ Dead routes (2026-08-28), do not re-attempt:** free chat-UI extraction (ChatGPT/Gemini
> returned nothing usable) and free-tier APIs *for extraction* (Groq 8,000 tok/min → ~8 h for a
> corpus run; Gemini 20 requests/day/model). The same tiers are **fine for judging**.

**Consolidated, paper-ready results.** Self-contained: every number, the methodology
needed to read it, and the honest caveats. Source of truth = the JSON summaries under
`output/` (paths given per table). Last updated 2026-08-27 (per-triple section; the KGE
sections below are unchanged since 2026-06-24).

---

## 1. What the system does (one paragraph)

We parse scientific-paper PDFs (ACL Anthology, arXiv, etc.) into structured text, extract
a knowledge graph of (subject, predicate, object) triples with an LLM (Qwen3-14B)
constrained by an ontology, and evaluate the resulting KG by an **unsupervised
citation-prediction** task: train a knowledge-graph embedding (KGE) on KG + paper–concept
links *with citation edges held out*, then test whether a paper's learned embedding ranks
its true citation neighbours above other papers. We compare **two ontologies** (CEO and
scinex), **three KGE models** (TransE, ComplEx, RotatE), and **two training objectives**
(margin-ranking vs. self-adversarial).

---

## 2. Corpus & extraction

| | Value |
|---|---|
| Parsed papers | **155** |
| Papers with a usable citation network (eval pool) | **132** |
| KGE training graph | ~14.3k nodes, 22 (CEO) / 25 (scinex) relations, ~54k triples |
| CEO fixed-extractor triples | **6,574** |
| scinex fixed-extractor triples | **6,776** (ratio 1.03× CEO) |
| Zero-triple papers | 2 (L16-1593, W14-5502 — empty under *both* ontologies; benign) |

Entity (subject) lists are built non-circularly from the **CS-NER** human-annotated
scientific-entity gazetteer intersected with each paper's text (≈70–150 entities/paper).
Both ontologies use the same entity lists; they differ only in the predicate vocabulary
(scinex = CEO + 5 relations, refined domain/range; 47 classes / 27 object properties).

---

## 3. Evaluation protocol

- **Task:** for each query paper, rank all candidate papers by embedding similarity;
  ground truth = its real citation neighbours (held out of training).
- **Metrics:** Hits@1/5/10 and MRR (mean reciprocal rank), reported as **mean ± std over
  5 random seeds**.
- **Two scorings, always reported together:**
  - **bidirectional** — predict *any* citation neighbour.
  - **temporal-filtered (`filt`)** — rank only candidates published ≤ the query's year
    (a paper can't cite the future); the more correct "reference-prediction" setup, on a
    smaller test set.
- **Honest model selection (added 2026-06-24):** the 132 eval papers are split into a
  fixed **validation half (66)** and **test half (66)** (`--split-seed 42`, independent of
  training seed). All tunable hyperparameters (γ in particular) are chosen on validation;
  final numbers are reported on the **untouched test half**.
- **Random baseline ≈ 0.2% Hits@1** over ~500 candidate papers → results should be read as
  **lift over random**, not against 1.0 (1.0 is not the target for unsupervised citation
  prediction).

---

## 4. ⭐ Headline result (defensible — clean val/test split)

**RotatE + self-adversarial loss, γ = 28 selected on validation, reported on the disjoint
test half (66 papers, 5 seeds).** File: `output/kge_multiseed_summary_test_*`.

| Ontology | Objective | Bidir MRR | Bidir Hits@10 | Filt MRR | Filt Hits@10 |
|---|---|---|---|---|---|
| CEO | margin (baseline) | 0.406 ± 0.053 | 0.727 | 0.418 ± 0.036 | 0.759 |
| **CEO** | **self-adversarial** | **0.598 ± 0.041** | **0.861** | **0.594 ± 0.027** | 0.844 |
| scinex | margin (baseline) | 0.391 ± 0.043 | 0.712 | 0.417 ± 0.027 | 0.737 |
| **scinex** | **self-adversarial** | **0.599 ± 0.023** | 0.845 | **0.606 ± 0.054** | 0.844 |

**Self-adversarial vs. margin on the same test split: +0.19–0.21 MRR.** The win survives
clean model selection (γ chosen on validation, not test).

> **Headline sentence for the paper:** *RotatE trained with self-adversarial negative
> sampling predicts held-out citation links with MRR ≈ 0.60 and Hits@10 ≈ 0.85 (5 seeds),
> with hyperparameters selected on a disjoint validation set.*

**Validation γ-sweep** (RotatE adv, val half) that selected γ=28: γ16 0.522 · γ20 0.580 ·
γ24 0.567 · **γ28 0.628** · γ32 0.619 (curve peaks at 28).

---

## 5. Full-corpus results (all 155 papers, no val/test split)

Use these as the "evaluated on the entire corpus" figures. The §4 test-split numbers are
the *primary* result (clean selection); these are the *complete-coverage* companion.

### 5a. Margin baseline — all three models (5 seeds, `kge_multiseed_summary*.json`)

| Model | CEO bidir MRR | CEO filt MRR | CEO H@10 | scinex bidir MRR | scinex filt MRR | scinex H@10 |
|---|---|---|---|---|---|---|
| TransE | 0.315 ± 0.008 | 0.334 ± 0.006 | 0.567 | 0.325 ± 0.005 | 0.330 ± 0.006 | 0.565 |
| ComplEx | 0.432 ± 0.018 | 0.456 ± 0.027 | 0.742 | 0.441 ± 0.028 | 0.472 ± 0.041 | 0.744 |
| RotatE | 0.403 ± 0.021 | 0.423 ± 0.025 | 0.708 | 0.391 ± 0.033 | 0.407 ± 0.019 | 0.703 |

→ Under the margin objective, **ComplEx is best**; RotatE and ComplEx are close.

### 5b. RotatE + self-adversarial — all 155 papers (γ=24, 5 seeds, `..._adv_*.json`)

| Ontology | Bidir MRR | Bidir H@10 | Filt MRR | Filt H@10 |
|---|---|---|---|---|
| CEO | 0.566 ± 0.026 | 0.847 | 0.555 ± 0.029 | 0.855 |
| scinex | 0.568 ± 0.022 | 0.838 | 0.575 ± 0.018 | 0.844 |

→ Same ~0.57 level; consistent with the cleaner §4 test-split result (~0.60).

---

## 6. Key findings (claims the data supports)

1. **Self-adversarial negative sampling is the decisive lever for RotatE** — +0.19–0.21
   MRR over margin (test split), lifting the best result from ~0.43 to ~0.60. This is the
   model's native training objective (Sun et al. 2019); plain margin loss under-trains it.
2. **The effect is model-specific.** Self-adversarial helps RotatE; it *hurt* ComplEx and
   TransE in tuning (their best remains margin). So the reported pipeline pairs RotatE with
   self-adversarial and leaves the others on margin.
3. **The two ontologies are statistically equivalent** on citation prediction (CEO filt
   0.594 vs. scinex filt 0.606 — within one std), at near-equal triple counts. The refined
   scinex ontology **matches CEO's downstream quality without degrading it**; it does not
   *beat* it on this metric.
4. **The temporal-filtered ("reference-prediction") task scores ~equal-or-higher** than
   bidirectional and is the more correct citation setup.
5. **All results are well above the ~0.2% random baseline** — a strong proof-of-signal that
   KG content learned purely from paper text predicts real citation links.

---

## 7. Honest caveats (state these in the paper)

- **Test-split numbers are on 66 papers** (half the eval pool) — the cost of an honest
  held-out validation split. The all-papers numbers (§5) cover 132 but had no separate
  tuning set.
- **Ontology result is a tie, not a win.** If the paper's contribution is the ontology
  comparison, citation prediction alone is too blunt to distinguish them; a per-triple
  precision or ontology-coverage analysis would be needed to say more.
  → **STILL A TIE on the second axis too (2026-08-27, n=240) — see "Per-triple extraction
  quality" below.** Per-triple judging on 18 papers gives corpus-weighted strict precision
  **CEO 34.7% vs scinex 39.6%**, bootstrap difference −4.9 pts with 95% CI [−22.5, +13.9].
  An earlier n=80 reading ("CEO beats scinex, 46.2 vs 34.4") **did not replicate and is
  withdrawn**. CEO does keep a lenient-precision edge (66.8% vs 59.9%). ⚠ Extractor was
  `qwen3:8b` on a laptop, NOT Qwen3-14B — not comparable to the KGE numbers above, and not
  the paper's figure. The KGE/citation-prediction tie is a different axis and is unaffected.
- **Report lift-over-random**, not absolute MRR vs. 1.0.
- **Self-adversarial hurts ComplEx/TransE** here, partly a mismatch with the cosine-similarity
  prediction path — don't over-generalise "self-adversarial is better."

---

## 8. Reproduction

```bash
# Headline: RotatE adv, val-selected γ=28, TEST split (per ontology)
python3 kge_multiseed.py --extractor fixed --model Qwen3-14B --kge rotate \
  --seeds 1 2 3 4 5 --epochs 1000 --loss adv --gamma 28 --adv-temp 1.0 \
  --eval-split test --split-seed 42 --val-frac 0.5 \
  --save-summary output/kge_multiseed_summary_test_ceo_adv.json \
  --run-dir output/kge_seedruns_test_ceo_adv
# (swap --extractor fixed_scinex + matching output paths for the scinex row;
#  swap --loss margin for the baseline rows)

# Re-select γ on validation:
#   ... --kge rotate --loss adv --gamma <G> --eval-split val   (pick best val MRR)
```

Result JSONs:
- Test split (headline): `output/kge_multiseed_summary_test_{ceo,scinex}_{margin,adv}.json`
- All papers, margin: `output/kge_multiseed_summary.json` (CEO), `..._scinex.json`
- All papers, RotatE adv: `output/kge_multiseed_summary_adv_{ceo,scinex}.json`

---

## 9. Open question — scale to more papers?

Currently 155 papers. **Recommendation: not required for the current claims.**
- More papers *lower* absolute MRR (bigger candidate pool = harder ranking) — they add
  scale/robustness, not a new finding. Going 79→155 already dropped absolute scores while
  conclusions held.
- Worth doing **only** if a reviewer would call 155 too small for the generalisation claim.
  The fetch→parse→enrich→extract→KGE chain is validated and can scale, but expect to
  re-frame around lift-over-random as the pool grows.
- The method/experiments are otherwise **complete and ready to write up**.

---

## Per-triple extraction quality — 18-paper handpicked corpus (PRELIMINARY, updated 2026-08-27 round 2)

> **⚠⚠ READ BEFORE QUOTING ANY NUMBER IN THIS SECTION.** The extractor was **`qwen3:8b` served by
> Ollama on a laptop GPU**, NOT the project-standard **Qwen3-14B**. These figures are **not
> comparable** to anything else in this file and are **not the paper's numbers**. They exist to find
> prompt/guard failures and to size the effect. The paper requires a VM re-run with Qwen3-14B.
> Judge = Claude (a different model family from the extractor); gold labels in
> `gold/sample_claude.csv` (n=80) + `gold/sample_round2_claude.csv` (n=160, disjoint ids).
> All tables below are reproduced by `python3 gold_report.py --labels gold/sample_claude.csv
> gold/sample_round2_claude.csv --bootstrap 2000 --out gold/report_combined.json`.

### Table 1 — Extraction volume (`relation` mode, 18 papers, both ontologies)

| | CEO (`relation`) | scinex (`relation_scinex`) |
|---|---|---|
| triples | 1,101 | 1,031 |
| distinct predicates | 21 | 24 |
| top-2 predicate concentration | **51%** (`addresses` 26.1 + `uses` 24.9) | **38%** (`addresses` 24.4 + `achieves` 14.0) |
| runs completed | 18/18 | 18/18 |

36/36 runs, 0 failures, ~5.5 h, 410 paragraphs. Yield ratio scinex/CEO = **0.94** — scinex extracts
the same volume, distributed over more relations (`uses` 24.9% → 8.8%; `comparesAgainst` 4.2% → 9.1%;
`configures` 0 → 6.2%; `produces` → 4.2%).

### Table 2 — Precision, judged (n=240 = 80 + 160 disjoint, stratified)

| ontology | n | CORRECT | PARTIAL | INCORRECT | strict (sample) | lenient (sample) | **strict (weighted)** | **lenient (weighted)** |
|---|---|---|---|---|---|---|---|---|
| CEO (`relation`) | 114 | 23 | 41 | 50 | 20.2% | 56.1% | **34.7%** | **66.8%** |
| scinex (`relation_scinex`) | 126 | 31 | 34 | 61 | 24.6% | 51.6% | **39.6%** | **59.9%** |
| *all* | 240 | 54 | 75 | 111 | 22.5% | 53.8% | — | — |

**Two weightings, because they disagree and both are honest.** The sample is stratified round-robin
over predicates, so rare relations are deliberately over-represented — its raw precision *understates*
the corpus. Re-weighting each predicate's observed precision by its true corpus frequency (covers
100% of triples) gives the weighted columns. **Always state which weighting is being quoted.**

**Bootstrap, 2,000 resamples within predicate buckets (corpus weights held fixed):**

| ontology | weighted strict | 95% CI |
|---|---|---|
| CEO | 34.8% | [21.7%, 48.8%] |
| scinex | 39.7% | [27.4%, 51.9%] |

### Table 3 — ⚠ The ontology comparison is a TIE, not a CEO win (supersedes the n=80 reading)

| gold set | CEO weighted strict | scinex weighted strict | reading |
|---|---|---|---|
| round 1 (n=80) | **46.2%** | 34.4% | CEO ahead by +11.8 |
| round 2 (n=160, disjoint) | 28.9% | **41.7%** | scinex ahead by +12.8 |
| **combined (n=240)** | 34.7% | 39.6% | **difference −4.9 pts, 95% CI [−22.5, +13.9]** |

**The round-1 CEO advantage did not replicate — it reversed sign on a disjoint draw of the same
size-class, and the combined difference straddles zero.** `P(CEO > scinex) = 0.30` under the
bootstrap. The honest statement is: *at n=240, CEO and scinex are indistinguishable on per-triple
precision, with an interval far too wide to separate them.* Session 24's "CEO beats scinex
corpus-weighted" headline is **withdrawn**; do not put it in the paper.

Why round 1 looked decisive: its CEO estimate rested on ~4 labels each for `addresses` (26% of the
corpus) and `uses` (25%) — half the CEO corpus mass carried by 8 labels. Those two cells moved down
in round 2 and took the whole weighted figure with them. **This is exactly the failure mode the
weighting warning predicted**, now observed.

CEO does keep a consistent **lenient** edge (66.8% vs 59.9%): its errors are more often "on-topic but
loosely typed", scinex's more often flatly wrong. That is a real qualitative difference and it does
*not* depend on the strict/tie result. The KGE/citation-prediction tie (§ above) is a separate axis
and is unaffected.

### Table 4 — Precision by predicate (guard worklist, n=240 combined)

Cells are 4–6 labels each; read the bands, not the individual percentages.

| band | CEO | scinex |
|---|---|---|
| **≥60% strict** | `achieves` 6/6, `comparesAgainst` 4/6 | `comprises` 5/6, `achieves` 4/6, `splitFrom` 3/5, `uses` 3/5 |
| **30–50%** | `encompasses` 2/5, `addresses` 2/6, `uses` 2/6, `designedFor` 2/6, `produces` 2/6 | `addresses` 3/6, `comparesAgainst` 3/6, `designedFor` 3/6, `produces` 3/6, `motivates` 1/3 |
| **≤20%** | `comprises` 1/6, `splitFrom` 1/6, `locatedIn` 1/6 | `mentions` 1/6, `achievesResult` 1/6, `producedBy` 1/6 |
| **0% strict** | `affiliatedWith` 0/6, `employs` 0/6, `evaluatedOn` 0/6, `trainedOn` 0/6, `evaluates` 0/6, `configures` 0/6, `publishedIn` 0/6, `supports` 0/6, `cites` 0/4, `reports` 0/2, `motivates` 0/1 | `employs` 0/6, `evaluates` 0/6, `configures` 0/6, `evaluatedOn` 0/6, `trainedOn` 0/5, `supports` 0/5, `extractedFrom` 0/6, `affiliatedWith` 0/5, `encompasses` 0/5, `cites` 0/4, `locatedIn` 0/4 |

**Stable across both rounds** (these are the fixes worth making):
- `achieves` is the strongest predicate in both ontologies — the metric/outcome backbone is sound.
- `employs`, `evaluates`, `configures`, `evaluatedOn`, `trainedOn`, `cites`, `affiliatedWith` score
  **0/6-ish in both rounds and both ontologies**. These are not sampling noise.
- `trainedOn` is the benign one: mostly PARTIAL, because the object is a generic phrase
  ("dataset of car images") rather than a named corpus.
- The **schema placeholder leak recurred** in round 2 — `(Merck, affiliatedWith, Organisation)` and
  `(FMC Corporation, affiliatedWith, Organisation)`, the literal class name as the object value. It
  is now confirmed on two predicates (`mentions`, `affiliatedWith`), so the fix must reject *any*
  object matching an ontology class name, not just patch `mentions`.
- New in round 2: **cross-reference objects** (`extractedFrom → "Section II DATA PREPARATION"`,
  `evaluatedOn → "Table III"`, `→ "ASSE 2025"` from a running header) — 6 of 240 labels. Rule 5 of
  the prompt already forbids section titles as objects; the guard does not enforce it.

### Table 5 — Structural defects of free-subject (`relation`) extraction, measured at corpus scale

| defect | rate | note |
|---|---|---|
| subject fragmentation | **6.2%** | `RT-DETR` / `Random Forest` / `Random Forest model`; collapses under "lowercase + strip trailing type noun" |
| figure/table-reference objects | **1.2%** | `evaluatedOn → Fig 4` |
| generic objects | **0.4%** | `produces → result` |
| exact duplicate triples | **9.3%** | collapsed automatically by `kg_builder` (edge-keyed): 2,351 parsed → 2,132 stored |
| clean objects | **98.4%** | — |

Free subjects cost ~6% node duplication, cheaply recoverable, and buy entities the CS-NER list does
not contain for these papers (`RT-DETR`, `SMOTE`, `UNIFAC`, `LSTM`). The trade looks favourable.

### Table 6 — Yield by corpus half (CEO)

| group | papers | triples | paragraphs | triples/paragraph |
|---|---|---|---|---|
| CS | 13 | 748 | 251 | **2.98** |
| non-CS | 5 | 353 | 159 | **2.22** |

**This is `relation` mode, where subjects are free** — so the gap reflects information density, *not*
entity-list coverage. The coverage question only bites in `fixed` mode, which is why the `fixed` run
is the informative next experiment (`hands_off.md` §22.4 documents the CS/non-CS entity-list split).

### Four fixable failure modes (`hands_off.md` §24.3)

1. **Domain/range violations** — the largest error class; `employs`/`affiliatedWith`/`locatedIn`/
   `publishedIn` used as generic verbs. Fix: post-parse type check per predicate.
2. **Direction inversions** — `splitFrom`, `supports`, `evaluates`, `configures`. Fix: extend the
   existing direction guards.
3. **⛔ Schema placeholder leak (bug)** — `(Kalman filter, mentions, AcademicPaper)`: the model copies
   an ontology *class name* out of the prompt as a value. Fix: reject objects matching a class name.
4. **Author-contribution sections mined** — CRediT statements produce `affiliatedWith` triples. Fix:
   skip `Author contributions` / `Declarations` / `Acknowledgements` at extraction time.

**Sample size caveat:** n=240 with 4–6 samples per predicate cell. Enough to rank the predicate
*bands* and to show the ontology difference is not resolvable; **not** enough to quote a per-predicate
percentage. The corpus-weighted figures carry a ±13-point 95% interval.

---

## ⭐⭐⭐ Table 8 — EXTRACTOR QUALITY UNDER ONE JUDGE (2026-08-28, Session 26)

**This is the cleanest result the project has.** Same 486 paragraphs, same system prompt, same
post-parse guards, same graph builder, **and now the same judge** — the only variable is the
extraction model. Judge: **`gemini-3.6-flash`** (a third family: not the extractor in either row,
so neither run is self-scored).

| extractor model | ontology | n judged | **weighted strict** | lenient |
|---|---|---|---|---|
| **claude-opus-5** | CEO | 100 | **89.1%**  [82.3, 94.5] | **97.9%** |
| qwen3:8b | CEO | 58 | **25.6%** | 50.4% |
| qwen3:8b | scinex | 52 | **11.7%** | 34.2% |

Verdict mix, claude-opus-5: **86 CORRECT / 13 PARTIAL / 1 INCORRECT** out of 100.
Bootstrap: 2,000 resamples within predicate buckets, corpus weights fixed.

**A ~3.5× strict-precision gap on identical input.** Combined with Table 7 (Claude emits 279 triples
where qwen emits 1,101), the picture is consistent and is the paper's story: **the stronger model
trades volume for precision, and the volume it declines to produce is the volume that was wrong.**

Per-predicate, claude-opus-5 (n≥6): `achieves` 100%, `comparesAgainst` 100%, `evaluatedOn` 100%,
`addresses` 92.3%, `uses` 84.6%, `designedFor` 66.7%, `comprises` 61.5% (5 PARTIAL, 0 INCORRECT).
**Every predicate that scored 0% strict for qwen3:8b scores ≥66% for Claude, or Claude simply never
uses it.** `affiliatedWith`, `employs`, `configures`, `publishedIn` and `cites` — the junk-generating
relations — appear **zero** times in Claude's output and were the largest 0%-strict blocks in qwen's.

> **⚠ Sample sizes differ** (100 vs 58/52) because the qwen judging run was cut off by the Gemini
> free-tier quota (20 requests/day/model) after 11 of 24 batches. The 110 collected verdicts are a
> stratified prefix of the same round-robin sample, not a biased subset, but the qwen row deserves
> the remaining 130 labels before it is final. Command to finish it is in `hands_off.md` §26.7.

### ⭐⭐⭐ Table 8a — ONTOLOGY x CAPABILITY: THE RANKING REVERSES (2026-09-03)

**Supersedes Table 3's "CEO vs scinex is a TIE".** That conclusion rested entirely on qwen3:8b, the
weakest extractor. Both ontologies at two capability levels, n=100 per cell, same guards, same code
version, same judge (`openai/gpt-oss-120b`):

| | CEO | scinex | scinex − CEO |
|---|---|---|---|
| **qwen3:8b (8B)** | 38.1% | **12.1%** | **−26.0 pts** → CEO wins |
| **qwen3-235b (235B MoE)** | 48.8% | **54.7%** | **+5.9 pts** → **scinex wins** |

**Interaction +31.9 points — the ranking inverts.** Volume agrees (scinex/CEO 0.94x at 8B, **1.48x**
at 235B), so at 235B scinex wins on both precision and volume. Mechanism: scinex is the richer schema
(27 relations vs 22, tighter domain/range) — more surface area to get wrong for a weak extractor, an
asset for a capable one. This also explains why the earlier CEO-vs-scinex comparison flipped sign
between disjoint gold rounds: it was noise-dominated at 8B.
⚠ Claim the **sign and the interaction**; the 8B magnitude moves with the sample (−11.5 pre-guards).

### ⭐⭐ Table 8b — THE HEADLINE UNDER A SECOND INDEPENDENT JUDGE (2026-08-28)

The same 100 claude-opus-5 triples, re-judged from scratch by **`openai/gpt-oss-120b`** (OpenAI
family, via Groq) — a third family, independent of both the extractor and the first judge.

| judge | family | weighted strict | lenient | sampled strict |
|---|---|---|---|---|
| `gemini-3.6-flash` | Google | **89.0%** | 97.9% | 86.0% |
| `openai/gpt-oss-120b` | OpenAI | **82.8%** | 92.6% | 85.0% |

**Near-identical sampled precision (86.0 vs 85.0).** Report the headline as a **range,
82.8-89.0% weighted strict**, not a point estimate. Either way, 3-4x qwen3:8b under one protocol.

Agreement between the two judges on these triples: raw **86.0%**, **κ 0.457 (3-class)**;
CORRECT-vs-not **91.0%**, **κ 0.637 (substantial)** — markedly higher than the 0.342 / 0.480 measured
on qwen's triples (Table 9). 81 of 100 called CORRECT by both.
**Judges agree far more about good extraction than about bad extraction; low inter-rater agreement is
itself a symptom of poor extraction.** `gold/agreement_claude_twojudges.json`.

### ⭐ Table 9 — FIRST INTER-RATER AGREEMENT (κ) IN THIS PROJECT

Claude and `gemini-3.6-flash` independently labelled **the same 110 qwen3:8b triples** under the same
rubric, neither seeing the other's verdict.

| measure | value |
|---|---|
| raw agreement (3-class) | 59.1% |
| **Cohen's κ (3-class)** | **0.342** (fair) |
| agreement (CORRECT vs not) | 83.6% |
| **Cohen's κ (binary)** | **0.480** (moderate) |
| precision assigned — Claude | 24.5% |
| precision assigned — Gemini | 13.6% |

Confusion matrix (rows = Claude, columns = Gemini):

| | CORRECT | PARTIAL | INCORRECT |
|---|---|---|---|
| **CORRECT** | 12 | 11 | 4 |
| **PARTIAL** | 3 | 9 | **25** |
| **INCORRECT** | 0 | 2 | **44** |

**The disagreement is almost entirely at the PARTIAL boundary.** The two judges agree on what is
flatly wrong (44 joint INCORRECT, and Gemini never called a Claude-INCORRECT triple CORRECT) and
largely on what is right; they diverge on "implied but not stated", which Gemini pushes to INCORRECT
25 times. That is why the binary κ (0.480) is well above the 3-class κ (0.342) — and it is the
argument for reporting **both** a strict and a lenient number, as this project already does, rather
than a single precision figure.

**This makes the Table 8 gap more robust, not less.** Gemini is the *harsher* judge — it scores qwen
at 13.6% where Claude scored it 24.5% — and Claude's extraction still earns 89.1% from it.

Reproduce:
```bash
python3 run_api_judge.py --slug gemini-judge --provider gemini --model gemini-3.6-flash
python3 judge_paste.py collect --csv gold/claude_sample_for_judge.csv --slug gemini-judge         --out gold/gemini_verdicts.csv
python3 gold_report.py --labels gold/gemini_verdicts.csv --model claude-opus-5 --bootstrap 2000
python3 gold_eval.py agree --gold gold/claude_labels_240.csv --b gold/gemini_verdicts_qwen.csv         --a-name claude --b-name gemini
```


### Table 7 — ⭐ Claude as the extraction backend, FULL CORPUS (same prompt, same guards) — 2026-08-27

`claude_extract.py` splits the extractor at the model boundary: it renders the **same** system+user
prompts `RelationOnlyExtractor` would have sent, and feeds the replies back through the **same**
`_parse_fixed_output()` guards and the **same** `KnowledgeGraphBuilder`. Only the model differs.
All 18 papers, 486 paragraphs, CEO ontology, `relation` mode.
Corpus JSON: `output/claude-opus-5_relation_corpus.json`; per paper under
`output/<id>/kg/relation/claude-opus-5/` (`prompts.jsonl`, `system_prompt.txt`, `responses.jsonl`,
`triples.json`, `kg.graphml`). Rebuild the table with `python3 claude_corpus_report.py`.

| | qwen3:8b (Ollama) | claude-opus-5 |
|---|---|---|
| papers × paragraphs | **20 × 525** | **20 × 525** |
| **triples stored (CEO)** | **1,185** | **296** (25% of the volume) |
| triples stored (scinex) | 1,117 | — |
| distinct predicates used | 21 | 15 |
| triples per paragraph | 2.26 | 0.56 |

> **Updated 2026-08-28 (Session 26): the corpus is now 20 papers for BOTH models.** The 2 added
> papers (`aiabstract2025`, `routepred2023` — substitutes, see below) contributed qwen 47+37 CEO /
> 57+29 scinex and Claude 12+5. The ratio is unchanged at 25%, on 39 extra paragraphs that neither
> model had seen — the volume gap is a stable property of the models, not an artefact of which
> papers were in the set. All 17 of Claude's new triples passed the guards with **zero rejections**.

> **Earlier the same session, 268 → 279 on the 18-paper set.** The `_is_valid_entity` bug below is now **fixed**,
> and Claude's stored `responses.jsonl` were re-ingested through the corrected builder — no new model
> calls. The 11 recovered triples are exactly the ones the bug was deleting:
> `(precision, evaluates, 0.923)`, `(accuracy, evaluates, 0.974)`, `(IoU, evaluates, 0.612)`,
> `(RMSE, evaluates, 14.76)`, `(root-mean-square error (RMSE), evaluates, 9.23)`,
> `(PNext, comprises, SF/SD/SP)`, `(the XGB algorithm, comparesAgainst, RF)`,
> `(GP, achieves, an R2 value greater than 0.97)`. `evaluates` goes **1 → 6** triples and
> `comprises` **25 → 28**. **qwen3:8b's 1,101 is NOT re-ingestable** — that run was driven through
> `kg_main` and never stored raw replies, so its equivalent losses can only be recovered by
> re-extraction. Its column therefore still carries the bug; the two columns are no longer strictly
> like-for-like on this axis.

**The volume gap is concentrated exactly where quality is worst.** Taking the 11 predicates that score
**0% strict** across the n=240 gold set:

| | share of that model's corpus |
|---|---|
| qwen3:8b | **22.7%** |
| claude-opus-5 | **6.0%** (16 of 268, pre-fix) |

> After the `_is_valid_entity` fix this row is stale in one direction: Claude now has 6 `evaluates`
> triples instead of 1 (21 of 279 = 7.5%). But `evaluates` was scored 0% strict *because of the very
> bug that deleted its correct instances* — the surviving ones were the malformed leftovers. It has to
> be **re-judged** before it belongs on a "predicates that are always wrong" list at all.

`affiliatedWith` (29 → **0**), `employs` (26 → **0**), `publishedIn` (8 → **0**), `configures`
(8 → **0**), `cites` (4 → **0**), `trainedOn` (54 → **1**), `evaluates` (12 → **1**). Conversely the two
predicates scoring ≥50% strict (`achieves`, `comparesAgainst`) rise from **16.9%** of the local
model's corpus to **29.5%** of Claude's. The stronger extractor is not just quieter — it declines
precisely the relations whose domain/range it cannot satisfy, and the CRediT/running-header/funding
sections that produced `affiliatedWith` and `supports` junk yield `{"triples": []}`.

> **⚠ NOT judged, and Claude must not judge them.** Extractor and judge must be different models or
> the evaluation is self-scoring. Use `run_local_judge.py` (qwen2.5, different family) or a human pass
> before quoting any precision figure for this run. The 240 gold labels cover qwen3:8b triples only.
> Volume and predicate mix are facts; **quality is still unmeasured.**

### ✅ FIXED 2026-08-28 — a second pipeline bug: `evaluates` could never be correct

`kg_builder._is_valid_entity()` requires every node to contain `[a-zA-Z]{2,}`, so a bare numeric
object is deleted **after** the guards pass. But the prompt defines `evaluates` as
`EvaluationMetric → ExperimentalResult` and its own GOOD example is `(<Metric>, evaluates, 88.5)`.
So the pipeline **asks for a value it then throws away**: `(precision, evaluates, 0.923)`,
`(RMSE, evaluates, 14.76)` and `(F1-score, evaluates, 9.23)` were all produced and all silently
dropped. Only the malformed, wordy `evaluates` triples survive into `triples.json` — which is why
`evaluates` scores **0% strict** in Table 4. The same filter deletes short acronym objects
(`< 3 chars`): `(PNext, comprises, SF/SD/SP)` vanished the same way.
**FIXED (Session 26).** `_is_valid_entity(text, predicate, role)` now takes the predicate and the
role, and admits a measurement value (`_VALUE_RE`: `88.5`, `0.923`, `14.76 ms`, `97%`, `1.2e-4`) as the
**object** of a value predicate (`evaluates`, `achieves`, `reports`) only — a bare number is still
rejected everywhere else, so the filter has not been loosened generally. All-caps acronyms (`SF`,
`SD`, `F1`) are exempted from the `len < 3` floor. Verified on 11 cases, then measured by re-ingesting
Claude's stored replies: **+11 triples, 268 → 279**, `evaluates` 1 → 6. Table 7 carries the detail.
qwen3:8b's column still predates the fix (no stored replies to re-ingest).

### ⛔ Guard bug found and fixed while running Table 7 (`kg_extraction/fixed_extractor.py`)

`_subject_in_sentence()` wrapped the subject in `\b…\b`. `\b` only marks a word/non-word transition,
so **any subject ending in a non-word character never matched** — `Segment Anything Model (SAM2)` is
dropped even when it appears verbatim in the source sentence. Since "Full Name (ABBR)" is the standard
way a paper introduces a model, this silently deleted nearly every parenthetical subject:
**2 of 2,132 triples** in the qwen3-8b corpus have a subject ending in `)`. Fixed with `_contains_term()`
(lookarounds applied only on the alphanumeric side). **All corpus numbers above were produced with the
buggy guard** — the fix can only add recall, and its effect is unmeasured until re-extraction.


---

> ### ⛔ CORPUS CORRECTION (2026-09-07) — THESE TABLES SAY 22 PAPERS; THE CORPUS IS 20
> `aiabstract2025` and `routepred2023` were **substitutes** for `tripplanner2020` and
> `linkpred2015`, added only while those were unobtainable. The user supplied both PDFs, so the
> substitutes are **retired** and the corpus is the user's **20-paper list**. Impact on what
> follows, counted from the verdict CSVs:
> Contamination, joining `verdicts.csv` × `index.csv` (the paper id is in **index.csv** — the
> triples `verdicts.csv` has no `paper` column, and filtering on it falsely reports zero):
> **triples 1–3 of 40 per model, paragraphs 0–2 of 16, C6 2 of 6 in every model.**
> **Restricting to the 20 papers moves every C1–C5 score by ≤4.5 points and changes no ordering**,
> so those tables stand as measured — C1 becomes 79.5 / 89.7 / 85.1 / 65.4 and C4 becomes
> 44.9 / **84.6** / 63.5 / 39.7.
>
> **C6 HAS BEEN REDRAWN AND RE-JUDGED on the 20-paper corpus** (2026-09-07), and the result
> **contradicts the C6 row of Table 10 and Finding 3 below**:
> qwen3-235b **50.0%**, gptoss-120b **25.0%**, gemma4-31b **33.3%** (was 16.7%),
> ministral-14b **8.3%** (was 0.0%). Ordered by volume — 367 → 50.0%, 547 → 25.0%,
> **615 → 33.3%**, 1,548 → 8.3% — the sequence is **NOT monotone**: gemma4-31b has more triples
> than gptoss-120b and a higher C6. **Spearman ρ = −0.80, not −1.00.** Report "C6 falls as volume
> rises (ρ = −0.80), though not strictly monotonically"; the "perfect rank correlation" claim in
> Finding 3 is **withdrawn**. Paragraph-coverage percentages below are over 567 paragraphs from
> 22 papers and need recomputing on the 20.

## ⭐⭐⭐ C1–C6 ONTOLOGY-QUALITY EVALUATION — COMPLETE (2026-09-05, Session 28)

**The first results under the six-criterion framework.** Four extractors, 22-paper corpus, 567
paragraphs, CEO ontology, all judged by **claude-opus-5 in-session** (never via OpenRouter).
Harness: `ontology_eval.py`. Raw verdicts: `ontology_eval/<slug>/ceo/{triples,paragraphs,papers}/verdicts.csv`.

**Sample: 40 triples + 16 paragraphs + 6 papers per model = 248 judgements**, the full sizes the
user asked for. Reproduce with `python3 ontology_eval.py report --model qwen3-235b gptoss-120b gemma4-31b ministral-14b`.

### Table 10 — C1–C6 by extractor (1.0 = best level, 0.5 = middle, 0.0 = worst)

| extractor | triples | paragraph coverage | C1 concept corr. | C2 concept compl. | C3 specificity | C4 relation corr. | C5 relation compl. | C6 consistency |
|---|---|---|---|---|---|---|---|---|
| qwen3-235b | 395 | 16.4% | 78.8% | 31.2% | 60.0% | 45.0% | 28.1% | **50.0%** |
| **gptoss-120b** | 583 | 42.9% | **88.8%** | **34.4%** | **75.0%** | **82.5%** | 31.2% | 25.0% |
| gemma4-31b | 684 | 51.7% | 85.0% | 28.1% | 72.5% | 62.5% | 25.0% | 16.7% |
| ministral-14b | **1,709** | **69.1%** | 63.7% | 31.2% | 55.0% | 38.8% | **34.4%** | **0.0%** |

### ⭐ Finding 1 — the models differ on TYPING, not on concept identification

C1 spans 63.7–88.8% (1.4×). **C4 spans 38.8–82.5% (2.1×).** The concepts extractors pull out are
mostly real and present in the sentence; what separates a good extractor from a bad one is whether
it respects the predicate's domain→range. Under the old single CORRECT/PARTIAL/INCORRECT rubric
these were one number and this split was invisible.

### ⭐⭐⭐ Finding 2 — VOLUME DOES NOT BUY COMPLETENESS. C2/C5 are flat.

ministral-14b produces **4.3× more triples** than qwen3-235b and fires on **4.2× more paragraphs**
(69.1% vs 16.4%). Its completeness scores: **C2 31.2% vs 31.2% — identical**; C5 34.4% vs 28.1%,
**+6.3 points**. Every model sits in a 25–34% band on both criteria.

This is the strongest result of the new framework and it was unobtainable before it. **Extracting
four times as much does not capture four times as much of the paper** — the extra volume is spent
re-stating what was already captured, enumerating pairwise combinations, and mining boilerplate,
not on the paragraphs that were being missed. Both the sparse and the dense extractor leave roughly
two-thirds of each paper's important content unrecorded, and they leave *different* thirds.

### ⭐⭐ Finding 3 — C6 falls monotonically with volume; rank correlation is perfect

| extractor | triples | C6 | verdicts (n=6 papers) |
|---|---|---|---|
| qwen3-235b | 395 | 50.0% | 1 CONSISTENT · 4 MINOR_ISSUES · 1 CONTRADICTORY |
| gptoss-120b | 583 | 25.0% | 3 MINOR_ISSUES · 3 CONTRADICTORY |
| gemma4-31b | 684 | 16.7% | 2 MINOR_ISSUES · 4 CONTRADICTORY |
| ministral-14b | 1,709 | **0.0%** | **6 CONTRADICTORY — every paper** |

More triples means more chances for two of them to conflict, and the conflicts are concentrated in
predictable places: **superlatives with the scope qualifier stripped** ("best AUC" asserted of three
different methods), **metric nodes bound to two values** because the class label was dropped, and
**surface-variant splitting** that lets incompatible claims attach to what should be one node.

The single clearest contradiction in the study: gemma4-31b asserts both `(VGG16) achieves (F1 w-avg
(0.23))` and `(VGG16) achieves (over 90% accuracy)`. The clearest structural violation: ministral-14b's
`(route prediction model) --comprises--> (route prediction model)`, a node containing itself.

> **⚠ C6 SCORING ARTIFACT — do not report C6 alone.** An empty triple set cannot contradict itself,
> so it scores CONSISTENT (1.0) vacuously. qwen3-235b's `textaug2023` (0 triples) is exactly this
> case and it is 1 of the 6 papers behind its 50.0%. C6 systematically rewards extracting nothing;
> it is only interpretable next to C2/C5, which price the emptiness.

### Finding 4 — recurring predicate failures, consistent across models

| predicate | defined as | what happens |
|---|---|---|
| `employs` | Organisation → Person | matched on the English verb for method-uses-method. **The only two correct uses in 160 triples were ministral's, and both came from author biographies** |
| `configures` | ExperimentalSpecification → Experiment | **direction wrong in 100% of observed uses** (gemma4 ×2, ministral ×2 plus 12 more inside judged paragraphs) — models write Model → setting |
| `splitFrom` | Dataset → Dataset | gptoss correct 2/2; gemma4, ministral and qwen3-235b all invert it — ministral 3/3 wrong in `gamlprop2025`, 4× in `linkpred2015` |
| `evaluatedOn` | Model/Experiment → Dataset | hardware (`Raspberry Pi 3 Model B`), other models, and evaluation protocols placed in the Dataset slot |
| `evaluates` | EvaluationMetric → ExperimentalResult | car brands, model names and parameter counts in the metric slot (ministral) |

### Finding 5 — boilerplate is mined as content, and C1–C4 cannot see it

Well-formed triples scoring CORRECT on all three per-triple criteria were drawn from: **author
biographies** (`(SOKENDAI) locatedIn (Japan)`, `(KMITL) employs (Rathachai Chawuthai)`),
**IEEE download watermarks** (`(KMITL) affiliatedWith (UniNet)`), **bibliographies**
(`(Keratocyte reflectivity and corneal haze) publishedIn (Experimental eye research)`),
**acknowledgements** (`(Assoc. Pannawit Samatthiyadikun) affiliatedWith (data analysis)` ×4), and the
**ACM CCS classification block**. The §26 boilerplate guard covers CRediT/funding/declarations but
not these. **A triple can be perfect on C1, C3 and C4 and still be worthless** — the criteria judge
the triple against its source sentence, and never ask whether that sentence was part of the paper.

### Finding 6 — one triple type the framework cleanly separates

ministral-14b's `roadwaylight2018#21` is the highest-yield paragraph in the study: **11 triples**,
8 of ~10 important concepts captured, C2/C5 both PARTIAL. **Every one of its nine `configures`
triples has the ontology direction wrong.** The same paragraph scores well on completeness and badly
on correctness — which is precisely the dissociation C1–C6 was adopted to expose and which a single
precision number cannot represent.

### ⚠ Caveats

- **n=40 triples / 16 paragraphs / 6 papers per model.** The C1/C3/C4 bands are reasonably sized;
  **C6 rests on 6 papers per model** and should be read as a trend across the four models (24 papers
  total, perfect rank correlation with volume), not as a per-model point estimate.
- **Single rater, by design.** claude-opus-5 in-session labelled every item. **This is a stated
  limitation of the C1–C6 track, not an unfinished step** (decided 2026-09-07): there is no second
  labeller and therefore no κ for C1–C6, and none is planned. The old-rubric κ figures (Table 9)
  measure a different rubric and do not transfer. Read every C1–C6 number as one expert rater
  applying a written rubric — criteria like C4's domain/range test involve genuine judgement calls
  (e.g. `(OC-SORT) publishedIn (CVPR 2023)` was scored PARTIAL on the grounds that a Model stands in
  for the required AcademicPaper; CORRECT and INCORRECT are both defensible readings), and their
  reproducibility across raters is unmeasured.
- **CEO only.** The scinex side of these four extractors has not been run.
- claude-opus-5 is also an extractor elsewhere in this project. It did **not** judge its own
  extraction here — the four extractors judged are all non-Claude.


---

## ⭐⭐⭐ FINAL C1–C6 RESULTS (2026-09-08, Session 31) — supersedes every table above

Post-junk-filter, post-redraw, 20-paper corpus, all eight runs on one code version.
Judge: claude-opus-5 in session, single rater. Reproduce with
`python3 ontology_eval.py report --model qwen3-235b gptoss-120b gemma4-31b ministral-14b --ontology ceo scinex`.

> ### ⛔ READ THIS BEFORE ANY COMPARISON — n≈40 IS THE BINDING CONSTRAINT
> Applying a filter that removed **0.7%** of triples and redrawing the samples moved scores by up to
> **+10.3 points** and **reversed the C1 top pair**. That is sampling variance, not the filter.
> **Any gap under ~10 points between two models is not distinguishable at this sample size.**
> Quote the C4 ranking (spread ~45 pts, unchanged by the redraw). **Do not quote a C1 ordering** —
> its whole spread across four models is ~24 pts, so neighbouring pairs sit inside the noise.

### Table 11 — CEO, all six criteria

| extractor | triples | C1 | C2 | C3 | C4 | C5 | C6 |
|---|---|---|---|---|---|---|---|
| `gptoss-120b` | 547 | 88.8% | 34.4% | 80.0% | **86.2%** | 31.2% | 25.0% |
| `gemma4-31b` | 615 | **93.8%** | 28.6% | 80.0% | 73.8% | 21.4% | 33.3% |
| `qwen3-235b` | 367 | 75.0% | 31.2% | 60.0% | 47.5% | 28.1% | **50.0%** |
| `ministral-14b` | 1,536 | 70.0% | 26.7% | 52.5% | 41.2% | **34.4%** | 8.3% |

n ≈ 40 triples / 15 paragraphs / 6 papers per model. **C2/C5/C6 were not redrawn** — only the triples
harness was affected by the filter — so they carry the pre-filter draw. ministral-14b's C6 is the
least trustworthy figure in the table: its triple sets changed slightly and its C6 rests on 6 papers.

### Table 12 — scinex (triples harness only) and the CEO→scinex deltas

| extractor | scinex vol | C1 | C3 | C4 | ΔC1 | ΔC3 | ΔC4 | vol ratio |
|---|---|---|---|---|---|---|---|---|
| `ministral-14b` | 1,538 | 72.5% | 60.0% | 46.2% | +2.5 | +7.5 | +5.0 | 1.00× |
| `qwen3-235b` | 563 | 72.5% | 57.5% | 46.2% | −2.5 | −2.5 | −1.2 | 1.53× |
| `gemma4-31b` | 525 | 88.5% | 79.5% | 59.0% | −5.3 | −0.5 | **−14.8** | 0.85× |
| `gptoss-120b` | 323 | 86.2% | 72.5% | **90.0%** | −2.5 | −7.5 | +3.7 | 0.59× |

**⚠ Most of these deltas sit inside the ±10 pt noise band.** Only gemma4-31b's **−14.8** on C4 clearly
does not. The honest statement is **"no consistent ontology effect was detectable at this sample
size"** — not that the effect is model-specific, which claims more than the data support.
Volume counts are exact rather than sampled, so the ratio column carries no sampling noise.

### The three findings, restated at the strength the data support

1. **Models differ on typing more than on finding concepts.** C4 spans ~45 points (41.2 – 86.2),
   C1 spans ~24 (70.0 – 93.8). The C4 ranking survived a full redraw unchanged. **Solid.**
2. **Volume does not buy completeness.** ministral-14b makes 4.2× qwen3-235b's triples and scores
   *lower* on C2 (26.7% vs 31.2%); all four models sit in a 21–34% band on both completeness
   criteria. **Solid** — the band is narrow and the direction is consistent.
3. **Coherence falls as volume rises.** Spearman ρ = −0.80 over four extractors, not monotone.
   **Directional only** — and C6 rests on 6 papers per model, so this is the weakest of the three.

### What the junk filter actually removed

40 triples of 6,054 (**0.7%**) — and **every one from ministral-14b** (12 CEO, 28 scinex). The other
three extractors lost nothing. Boilerplate mining — author bios, CRediT lines, data-availability
statements, a fictional UX persona, OCR table debris — is a property of the weakest extractor in the
set, not a general property of extraction.
